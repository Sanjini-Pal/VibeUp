import 'package:flutter/material.dart';

class singer7 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Second Screen'),
      ),
      body: Center(
        child: Text(
          'Null',
          style: TextStyle(fontSize: 24),
        ),
      ),
    );
  }
}